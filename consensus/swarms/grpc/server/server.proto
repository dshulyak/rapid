syntax = "proto3";

option go_package = "github.com/dshulyak/rapid/consensus/swarms/grpc/server";

package server;

import "consensus/types/types.proto";

message Empty {}

service Server {
  rpc Send(types.Message) returns (Empty) {}
}
