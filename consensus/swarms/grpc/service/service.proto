syntax = "proto3";

option go_package = "github.com/dshulyak/rapid/consensus/swarms/grpc/service";

import "consensus/types/types.proto";

message Empty {}

service Consensus {
  rpc Send(types.Message) returns (Empty) {}
}
