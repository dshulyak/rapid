syntax = "proto3";

option go_package = "github.com/dshulyak/rapid/consensus/network/grpc/service";

import "consensus/types/types.proto";

message Empty {}

service Consensus {
  rpc Send(types.Message) returns (Empty) {}
}
